kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  apiServerPort: 33894   # unique port so it doesn't clash with argocd-cluster
nodes:
  - role: control-plane
    image: kindest/node:v1.33.1
